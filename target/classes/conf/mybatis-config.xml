<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 命名空间 -->
    <typeAliases>
        <typeAlias type="com.springmvc.cms.model.Student" alias="Student"/>
    </typeAliases>

    <!-- 映射map -->
    <mappers>
       <!-- <mapper resource="/mapper/StudentMapper.xml"/>--> <!--路径配置可能有问题，要注意下-->
    </mappers>
</configuration>